<%= turbo_stream.prepend("flash-messages", partial: "layouts/shared/flash", locals: { flash: flash }) %>
<%= turbo_stream.update(dom_id(transaction, :status), transaction.status.titleize) %>
<%= turbo_stream.remove(dom_id(transaction, :cancel_link)) %>
